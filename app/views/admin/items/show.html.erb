<main>
<div class="container px-5 px-sm-0">

    <h4>商品詳細</h4>
     <div class="row">
      <div class="col-xl-4">
      <%= image_tag @item.get_item_image(300,300) %>
      </div>   
      <div class="col-xl-3">
      <p>商品名：<%= @item.name %></p>
      <p>商品説明：<%= @item.introduction %></p>
      <p>税込価格：<%= (@item.price.to_i * 1.1).floor %>(<%= @item.price %>)円</p>
      <p>ジャンル: <%= @item.genre.name %></p>
      <p>販売ステータス：
      <% if @item.is_active == true %>
       販売中
      <% else %>
       販売停止中
      <% end %>
      </p>
      <%= link_to "編集する", edit_admin_item_path %>
      </div> 
    </div>
    
  
  </div>
</main>  
