<h1>商品一覧</h1>
<% @items.each do |item| %>
<div>
  <%= link_to item_path(item.id) do %>
      <%= image_tag item.get_item_image(100,100) %>
  <% end %>
  
  <p><%= item.name %></p>
  <p>¥<%= item.price %></p>
</div>
<% end %>
<%= paginate @items %>